@model ShortenModel

@{
    Layout = "_Layout";
}

<div class="min-w-full min-h-screen flex items-center justify-center">
    <div class="flex">
        <div class="bg-gray-200 rounded-l-lg text-2xl py-4 px-8 hover:bg-gray-300">@Model.ShortenedUrl</div>
        <button type="submit" class="text-2xl bg-blue-500 hover:bg-blue-700 text-white font-bold py-4 px-8 rounded-r-lg">Copy</button>
    </div>
</div>